{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "start": "lerna bootstrap",
    "test": "lerna test",
    "registry": "lerna publish --registry http://localhost:4873"
  }
}
